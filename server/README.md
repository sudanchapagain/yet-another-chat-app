you should have these to env var set

```sh
export DATABASE_URL=postgres://postgres:root@localhost:5432/yacc
export JWT_SECRET=
```

todo:

- re do stuff (make use of config instead of calling getenv by modules)
